<Window x:Class="AthStitcher.Views.ChangePasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Change Password" Height="240" Width="400" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="140"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <TextBlock Grid.Row="0" Grid.Column="0" Text="Username:" VerticalAlignment="Center"/>
    <TextBlock Grid.Row="0" Grid.Column="1" Name="UsernameLabel"/>

    <TextBlock Grid.Row="1" Grid.Column="0" Text="Current Password:" VerticalAlignment="Center"/>
    <PasswordBox Grid.Row="1" Grid.Column="1" Name="CurrentPassword"/>

    <TextBlock Grid.Row="2" Grid.Column="0" Text="New Password:" VerticalAlignment="Center"/>
    <PasswordBox Grid.Row="2" Grid.Column="1" Name="NewPassword"/>

    <TextBlock Grid.Row="3" Grid.Column="0" Text="Confirm Password:" VerticalAlignment="Center"/>
    <PasswordBox Grid.Row="3" Grid.Column="1" Name="ConfirmPassword"/>

    <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button Content="OK" Width="80" Margin="0,0,8,0" IsDefault="True" Click="Ok_Click"/>
      <Button Content="Cancel" Width="80" IsCancel="True"/>
    </StackPanel>
  </Grid>
</Window>
